"use client";

const AddToCart = () => {
  return (
    <div>
      <button
        className="btn btn-secondary rounded-2xl"
        onClick={() => console.log("Clicked!")}
      >
        AddToCart
      </button>
    </div>
  );
};

export default AddToCart;
